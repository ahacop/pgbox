# Auto-generated from pgbox-data - PostgreSQL 17

extension = "tablelog"
package = "postgresql-17-tablelog"
description = "log changes on tables and restore tables to point in time"

[image]
apt_packages = ["postgresql-17-tablelog"]

# SQL initialization
[[sql.initdb]]
text = "CREATE EXTENSION IF NOT EXISTS tablelog;"
